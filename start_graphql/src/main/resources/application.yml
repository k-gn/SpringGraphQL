spring:
  h2:
    console:
      enabled: true

  graphql:
    graphiql:
      # graphiql? true? ??? ?? localhost:8080/h2-console? ?? localhost:8080/graphiql ??
      # graphql ??? ???? ?????.
      # ? ?? ??? IntelliJ? GraphQL ????? ???? IntelliJ?? ?? ???? ???? Postman??? ?????.
      enabled: true
      printer:
        # jpa show ?? graphql ??? ?????.
        enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/test
    username: sa
    password: 1234

  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop
      jdbc:
        time_zone: Asia/Seoul
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true